index:
  embeddings_model: "sentence-transformers/all-MiniLM-L6-v2"
  chunk_size: 200
  overlap: 40
  n_chunks: 200
  use_gpu: false
retrieval:
  top_k: 3
generation:
  model_name: "gpt2"  # CPU-safe; swap to GPU LLM later
  max_new_tokens: 64
  use_gpu: false
metrics:
  exporter_port: null
data:
  synthetic_seed: 42
